using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Music : MonoBehaviour
{
    public AudioSource zerarTrack;
    // Start is called before the first frame update
    void Start()
    {
        zerarTrack.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
